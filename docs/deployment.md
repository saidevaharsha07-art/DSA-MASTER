# Production Deployment Guide

## Guidelines
- Containerized via multi-stage `Dockerfile`.
- Run locally or on-prem via `docker-compose up -d`.
- Zero cloud secret requirements for mock/local execution.
